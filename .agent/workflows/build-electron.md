---
description: Build and package the Electron application for Windows
---
This workflow builds the Next.js app and packages the Electron application into the `dist` folder.

1. Install dependencies
// turbo
npm install

2. Build and package the app
// turbo
npm run electron-build
